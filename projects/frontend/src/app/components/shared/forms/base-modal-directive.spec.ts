import { BaseModalDirective } from './base-modal-directive';

describe('BaseModalDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseModalDirective();
    expect(directive).toBeTruthy();
  });
});
