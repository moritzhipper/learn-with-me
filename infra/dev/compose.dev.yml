services:
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    shm_size: 128mb
    env_file:
      - ../../.env

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
